/**
 * DO NOT RUN: this script previously overwrote full logos with text-only PNGs.
 * Restore assets from git (e8bab9d) and run scripts/recolor-logo-wordmark.mjs instead.
 */
console.error(
  "Refusing to overwrite tropicash-logo-*.png. Restore from git and use scripts/recolor-logo-wordmark.mjs."
);
process.exit(1);
