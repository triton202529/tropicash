/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Minimal production defaults; safe on Vercel (no experimental flags).
  poweredByHeader: false,
};

export default nextConfig;
